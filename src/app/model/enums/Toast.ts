export enum toastStates {
    INFO = 'tertiary',
    SUCCESS = 'success',
    WARNING = 'warning',
    DANGER = 'danger'
}

export enum cameraStatements {
    CAMERA_ERROR = 'Camera is not available',
    CAMERA_UNSUCCESSFULL_PICTURE = 'Taking picture unsuccessful'
}

export enum gameServiceStatements {
    INVALID_ID = 'Invalid user id',
    INVALID_PLAYER = 'Invalid player name',
    SOCKET_CLOSED = 'Socket is closed'
}